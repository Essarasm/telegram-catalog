-r requirements.txt
pytest>=8.0
pytest-cov>=4.0  # test-only — `pytest --cov=backend --cov=bot` for coverage tracking
ruff>=0.6
vulture>=2.13
xlwt>=1.3.0  # test-only — synthesize .xls fixtures for 1C XLS importer tests (balances/debts/prices)
